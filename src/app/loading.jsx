const loading = () => {
  return (
    <div
      style={{
        height: '100vh',
        width: '100%',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <h3 style={{ textAlign: 'center' }}>Loading...</h3>
    </div>
  );
};

export default loading;
